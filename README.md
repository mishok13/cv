This is my resume, written in LaTeX. Not much more to say about it.

To compile, use `pdflatex`:

```
$ pdflatex cv.tex # produces cv.pdf
# Alternatively, generate short resume
$ pdflatex resume.tex
```
